# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
dist/
build/
*.egg
.eggs/

# Virtual environments
.venv/
venv/
env/

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db

# Environment / secrets
.env
.env.local

# REAPER working files
*.reapeaks
**/Backups/

# Local working notes (not for repo)
local/
notes/

# Coverage
.coverage
htmlcov/

# Audio output (generated)
output/

# Node
node_modules/

# Audio stems and samples (copyrighted, not distributable)
examples/all_the_small_things/
songs/*
# Exception: packaged songs (fully tracked)
!songs/example-songs/
# For other song dirs: .prompt files are source of truth, .ts are AI-generated
!songs/**/*.prompt

# Rust
target/

# Build/working artifacts
.cache
clickbait-workspace/

# Claude Code worktrees
.claude/worktrees/

# Demo recordings (generated)
demo/output/

# Practice bundles (generated; large, regenerated per-render)
bundles/

# Skill: generated file and downloaded binary
.claude/skills/clickbait/SKILL.md
.claude/skills/clickbait/bin/
skill/bin/

# Claude Code project settings (generated by setup.sh per user)
.claude/settings.json

# Sandbox: simulated fresh user install for skill testing
sandbox/

# Local song backups (authored files snapshot before schema migration)
backups/

# Local practice launcher (symlinks into gitignored songs/)
practice/

# Personal dogfooding tool, not part of the project
bin/sync-band.sh

# Compiled esbuild bundles — regenerable via `npm run build` (scripts/build.mjs).
# Every bin/*.mjs is build output; bin/sync-band.sh (personal) is ignored above.
bin/*.mjs

# Scratch / experiments
tmp/
# Demucs stems are large + regenerable; keep out of git even under example-songs
songs/example-songs/stems/

# Personal working notes — kept local, not in the repo
TODO.md
